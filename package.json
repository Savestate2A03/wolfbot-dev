{
  "name": "wolfbot-dev",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node esbuild.config.mjs",
    "deploy": "npm run build && npx tsx scripts/deploy.ts",
    "deploy:guided": "npm run build && npx tsx scripts/deploy.ts --guided",
    "deploy:commands": "npx tsx scripts/deploy-commands.ts",
    "lint": "eslint src/ scripts/"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.1030.0",
    "@aws-sdk/client-lambda": "^3.1030.0",
    "@aws-sdk/client-scheduler": "^3.1043.0",
    "@aws-sdk/client-secrets-manager": "^3.1043.0",
    "@aws-sdk/lib-dynamodb": "^3.1030.0",
    "@types/aws-lambda": "^8.x",
    "@types/luxon": "^3.7.1",
    "@types/node": "^24.12.2",
    "esbuild": "^0.25.x",
    "eslint": "^9.x",
    "tsx": "^4.x",
    "typescript": "^5.x",
    "typescript-eslint": "^8.x",
    "undici": "^8.1.0"
  },
  "dependencies": {
    "chrono-node": "^2.9.1",
    "luxon": "^3.7.2"
  }
}
